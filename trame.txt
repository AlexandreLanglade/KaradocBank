-------------KARADOCBANK-------------

///INITIALISATION///
    lecture jason()
    init(param_jason)
        init_LC_Comptes()
        init_LC_Client()
        init_LC_Virement()
//FIN INITIALISATION//

///AUTHENTIFICATION///
    authentification()
        recherche_id_client()
        verification_mdp()
        if(id-client == client) then open_menu_client()
        if(id_client == admin) then open_menu_admin()
    else nouveau_client()
        save_nouveau_client()
//FIN AUTHENTIFICATION//

///MENU ADMIN///
    if(choix() == gestion_des_comptes)
        - consulter_requetes() //Je suis pas sur de comment on gere les requetes pour le moment je le mettrais la
        - modifier_compte() //permetrais aussi de supprimer le compte
        - afficher_liste_comptes()
    if(choix() == gestion_des_clients)
        - ajouter_client()
        - modifier_coor_client()
        - afficher_liste_titulaires_compte()
    if(choix() == administration)
        modifier_mdp_admin()
//FIN MENU ADMIN//

///MENU CLIENT///
    if(choix() == gestion_des_comptes)
        - consulter_le_solde()
        - consuler_liste_operations()
        - effectuer_virement()
    else
        - changer-mdp()
        - demander_creation_compte()
        - demander_suppression_compte()
//FIN MENU CLIENT//

--------------------------------------